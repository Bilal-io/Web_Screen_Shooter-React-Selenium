import styled from "styled-components";

import NormalImg from "../Img";

const Img = styled(NormalImg)`
  height: 48px;
  width: 48px;
  color: "#0a0a0a";
  margin-right: 8px;
`;

export default Img;
